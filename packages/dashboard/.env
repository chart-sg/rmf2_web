# because we are a monorepo, some create-react-app dependencies may be installed in the root directory,
# it complains if it find such packages so we need to disable the check.
SKIP_PREFLIGHT_CHECK=true

REACT_APP_TRAJECTORY_SERVER=ws://10.233.29.67:8006
REACT_APP_RMF_SERVER=http://10.233.29.67:8000
# REACT_APP_AUTH_PROVIDER=keycloak
# REACT_APP_KEYCLOAK_CONFIG='{"realm": "rmf-web","clientId": "rmf_api_server","url": "https://keycloak.ctfportal.com/auth"}'
